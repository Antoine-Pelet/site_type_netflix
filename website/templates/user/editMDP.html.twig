{% extends 'base.html.twig' %}

{% block title %}Edit MDP User{% endblock %}

{% block stylesheets %}
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
	<link href="{{ asset('style.css') }}" rel="stylesheet"/>
	<link href="{{ asset('styleEdit.css') }}" rel="stylesheet"/>
{% endblock %}

{% block body %}
<div class="d-flex flex-column align-items-center">
    <div class="formu align-items-center">
        <h1>Edit MDP User</h1>
        <div class="mb-3">
            {{ form_row(form.plainPassword, {label: 'Password'}) }}
            {{ form_errors(form.plainPassword) }}
        </div>
    </div>
        {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

    <button class="btn" href="{{ path('app_user_index') }}">back to list</button>
</div>
{% endblock %}
